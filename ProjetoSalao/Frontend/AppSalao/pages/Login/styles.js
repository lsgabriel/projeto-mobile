import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container:{
        height:500,
        backgroundColor: 'linear-gradient(to bottom right, white, blueviolet)',
    },
    textStyle:{
        alignItems:'center',
        color:'lightgray',
        fontSize:35,
    },
});

export default styles;